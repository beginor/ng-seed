import { BackdropBlurDirective } from './backdrop-blur.directive';

describe('BackdropBlurDirective', () => {
    it('should create an instance', () => {
        const directive = new BackdropBlurDirective(null as any);
        expect(directive).toBeTruthy();
    });
});
